const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],
	theme: {
		extend: {
			fontFamily: {
				teyvat: ['Teyvat'],
				khaenriah: ['Khaenriah']
			}
		}
	},
	plugins: []
};

module.exports = config;
